apiVersion: v1
kind: ConfigMap
metadata:
  name: registration-service-config
  namespace: events-platform
data:
  PORT: "4003"
  DB_HOST: "mssql"
  DB_PORT: "1433"
  DB_NAME: "eventsdb"
  DB_ENCRYPT: "false"          # local SQL Server has no valid cert; Azure uses "true" (see .env.example)
  DB_TRUST_SERVER_CERT: "true"
  EVENT_SERVICE_URL: "http://event-service:4001"   # cluster-internal DNS name of the Event Service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: registration-service
  namespace: events-platform
spec:
  replicas: 1
  selector:
    matchLabels:
      app: registration-service
  template:
    metadata:
      labels:
        app: registration-service
    spec:
      containers:
        - name: registration-service
          image: registration-service:local
          imagePullPolicy: Never
          ports:
            - containerPort: 4003
          envFrom:
            - configMapRef:
                name: registration-service-config
          env:
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: mssql-secret
                  key: DB_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: mssql-secret
                  key: DB_PASSWORD
          readinessProbe:
            httpGet:
              path: /health
              port: 4003
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 4003
            initialDelaySeconds: 10
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: registration-service
  namespace: events-platform
  labels:
    app: registration-service
spec:
  selector:
    app: registration-service
  ports:
    - name: http
      port: 4003
      targetPort: 4003
  type: ClusterIP
