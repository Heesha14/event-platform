# Server
PORT=4004

# ClickHouse (runs locally via docker-compose, unlike the other services
# which point at Azure SQL Database - analytics data isn't transactional).
CLICKHOUSE_URL=http://localhost:8123
CLICKHOUSE_DB=analytics
CLICKHOUSE_USER=default
CLICKHOUSE_PASSWORD=
